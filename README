In order to build, we're using cabal:

./Setup.lhs configure
./Setup.lhs build
./Setup.lhs install

Or you can run the binaries yourself from within dist/build/

dist/
  compiled binaries, documentation etc.

src/
  source for binaries + utilities

Decaf/
  compiler codebase

testsuite/
  tests

TODO:
error out on bad parse
  - keywords/identifiers start with a number/end with an invalid character/have an invalid character
  - major refactoring
  - merge this repo with scott's work.
