class Program {
  int get_int(int x) {
    return x;
  }

  boolean get_bool(boolean x) {
    return x;
  }

  void main() {
    int a, b, c, d;
    boolean x;

    a = get_int(2);
    b = get_int(3);
    x = get_bool(true);
    c = 0; d = 0;

    if (x) {
      c = a + b;
    }
    d = a + b;

    callout("printf", "%d\n", c);
    callout("printf", "%d\n", d);
  }
}
